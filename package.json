{
  "name": "chainscribe-enterprise",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@chainscribe/web",
    "build": "npm run build --workspace=@chainscribe/web",
    "lint": "npm run lint --workspace=@chainscribe/web",
    "type-check": "npm run type-check --workspace=@chainscribe/web"
  }
}